# AGENTS

이 저장소에서 작업을 시작하기 전에 아래 순서로 파일을 먼저 읽으세요.

1. `README.md`
2. `docs/PROJECT.md`
3. `docs/ARCHITECTURE.md`
4. `docs/PROVIDERS.md`
5. `docs/CONVENTIONS.md`
6. `docs/STAGES.md`

작업 원칙:
- README와 실제 코드(`src/`, `config/`)를 기준으로 판단하고, 코드에 없는 내용은 문서화하지 않습니다.
- API 제한 보수 운영(일일 예산/리저브)을 우선합니다.
- 실행 중단 후 재개 가능하도록 상태 파일(`data/state/state.json`) 흐름을 유지합니다.

작업 완료 규칙:
- 작업을 마치면 반드시 `docs/STAGES.md`를 현재 상태에 맞게 업데이트하세요.
